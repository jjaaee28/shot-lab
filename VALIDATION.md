# 촬영 구도 실험실 — 검증 결과 보고서 (VALIDATION.md)

> 최근 확인: 2026-09-20. 로컬 프로덕션 빌드는 성공했으며, 개발 서버의 첫 화면·실험 화면·주변 돌기 재생 완료·좌측 1/3 프레이밍·3분할 가이드·상단 1/3 세로 프레이밍을 브라우저에서 다시 확인했다.

## 1. 프로젝트 개요 및 상태
- **프로젝트명**: 촬영 구도 실험실 (SHOT LAB)
- **목표**: 영화/영상 촬영 입문자가 같은 장면(의자에 앉아 책을 읽는 인물)에서 카메라 앵글 및 무빙에 따른 화면 구도 변화를 직관적으로 체험하고 이해할 수 있는 웹 목업
- **기술 스택**: Next.js 16 (App Router), React 19, Three.js, Pure CSS
- **현재 상태**: 구현 완료 및 로컬 환경 검증 통과 (빌드 및 런타임 테스트 완료)

---

## 2. 검증 항목 및 결과

### 1) 빌드 및 패키지 검증 (Automated Build Test)
- **실행 명령**: `next build`
- **결과**: `✓ Compiled successfully in 2.7s`, `✓ Generating static pages (3/3)`
- **평가**: TypeScript/JavaScript 문법 오류, 누락된 모듈, React SSR/Hydration 충돌 없이 완전하게 정적 최적화 빌드 성공.

### 2) 로컬 개발 서버 기동 및 엔드포인트 검증 (Runtime HTTP Test)
- **실행 명령**: `next dev --hostname 127.0.0.1 --port 3000`
- **검증 내용**: `http://127.0.0.1:3000` HTTP GET 요청
- **결과**: `Status 200 OK`, HTML 렌더링 정상 수신 (`Content-Length: ~13KB`)

### 3) 킬러 기능 및 인터랙션 구현 검증
| 조건 | 사용자 행동 | 기대 결과 및 구현 검증 | 결과 |
|---|---|---|---|
| **화면 전환** | '촬영 방법 둘러보기' 클릭 | 소개 화면에서 작업대(실험실) 화면으로 매끄럽게 전환 | **통과** |
| **고정 각도 프리셋** | 정면, 측면, 높은 위치, 낮은 위치 클릭 | 각 앵글에 맞춰 카메라 높이(Elevation)와 각도(Azimuth) 즉각 반영, 정지 구도 유지 | **통과** |
| **360° 및 상하 각도 조절** | 뷰어 영역 마우스 드래그(상하좌우) 또는 방향키(←, →, ↑, ↓) 조작 | 인물을 중심으로 수평 공전 및 상하 높이(Elevation) 실시간 반영 (방위각 및 고도각 수치 동시 표시) | **통과** |
| **달리 인 (Dolly In)** | '달리 인' 선택 | 5초 동안 카메라가 인물 쪽으로 전진(Radius 6.2 → 3.3)하며 화면에 확대 표시 | **통과** |
| **달리 아웃 (Dolly Out)** | '달리 아웃' 선택 | 5초 동안 카메라가 인물로부터 후퇴하며 주변 공간(창문, 탁자) 시야 확보 | **통과** |
| **인물 주변 돌기 (우/좌)**| '주변 돌기 (우)' 또는 '주변 돌기 (좌)' 선택 | 10초 동안 인물을 바라보며 선택한 방향(시계방향 또는 반시계방향)으로 360° 회전 무빙 수행 | **통과** |
| **인물 사이드 프레이밍** | '좌측 1/3', '우측 1/3', 마우스 우클릭 좌우 드래그, 슬라이더 조작 | 카메라가 수평으로 트럭킹(Trucking)하여 인물을 화면 사이드에 배치(Looking Room 연출), 3분할 가이드라인과 완벽 정렬 | **통과** |
| **3분할 격자 가이드 (Rule of Thirds)** | '# 3분할 선' 버튼 클릭 | 뷰어 화면에 시네마 3x3 구도 격자선 표시/숨김 토글 | **통과** |
| **카메라 줌 인/아웃** | 마우스 휠, 핀치 제스처, 슬라이더, +/− 버튼 조작 | 광각(0.5x)부터 익스트림 망원(3.0x)까지 왜곡 및 클리핑 없이 부드러운 줌 인/아웃 및 1.0x 리셋 | **통과** |
| **재생 및 상태 제어** | 일시정지, 계속 재생, 방향 변경 후 재생 | 재생바 진행률 표시, 중간 정지 시 새 방향 설정 후 재개 지원 | **통과** |

### 4) 시각 디자인 및 모바일 반응형
- **색상 체계**: 베이지 바탕(`--paper: #F2E9DA`)과 먹색 선(`--ink: #161616`)의 일관된 미니멀 라인아트 그래픽 적용
- **레이아웃**: 데스크톱에서는 16:9 대형 뷰어와 우측 컨트롤 패널의 그리드 배치, 760px 이하 모바일 화면에서는 세로 스택 구조로 자동 전환
- **터치 지원**: 모바일 화면 세로 스크롤과 3D 뷰어 터치 드래그(`touch-action: pan-y`) 충돌 방지 및 버튼 최소 터치 영역(44px 이상) 준수

---

## 3. 한계점 및 추후 확장 과제 (Out of Scope)
1. **렌즈 및 조명 시뮬레이션**: 본 목업은 순수 '카메라의 위치와 구도(Framing)'를 전달하는 것에 집중되어 있으며, 실제 렌즈의 초점거리(FL), 심도(DoF), 조명 셋업은 포함하지 않습니다.
2. **실제 비디오 스트리밍/AI 영상 생성 없음**: 모든 장면은 브라우저 클라이언트 WebGL(Three.js) 기반으로 실시간 렌더링되며, 외부 서버나 유료 API 연결이 필요 없습니다.
3. **독립 용어 사전 미포함**: 기획에 따라 복잡한 사전 페이지 대신, 촬영 방법 선택 버튼에 직관적인 동작 설명과 영문 표기를 함께 배치하여 직관적으로 파악하도록 구성했습니다.
